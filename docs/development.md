# Development

The initial migration scaffold is intentionally dependency-free. Validate it
with the existing host checks and keep `aegis-core` as the default workspace
member until a migrated crate is ready to replace a responsibility.
